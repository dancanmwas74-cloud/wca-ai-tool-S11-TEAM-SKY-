# group-project
This is project of our group about car spare parts name "Spare parts hub"
It composes the spare parts available and their prices.
The user sends the prompt about the spare parts we have and their price.
The system checks the API and generates the response
The response generated checks the instructions and generates the output.
